<!DOCTYPE html>
<html lang="en">
    <%- include('./head.ejs') %>
<body>
    <%- include('./navbar.ejs') %>
    <div class="container">
        <div class="card my-4">
            <img 
                src="<%= blog.poster %>" 
                class="card-img-top" 
                alt="<%= blog.title %>"
                style="height: 600px; object-fit: cover"
            >
            <div class="card-body mt-4">
                <h5 class="card-title text-center"><%= blog.title %></h5>
                <p class="card-text text-center"><%= blog.createdAt %></p>
                <p class="card-text p-5"><%= blog.text %></p>
            </div>
        </div>
    </div>
</body>
</html>